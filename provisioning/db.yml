---
- hosts: all
  remote_user: vagrant
  sudo: yes

  vars_files:
    - vars/redis.yml
    - vars/mysql.yml
    - vars/postgresql.yml

  pre_tasks:
    - apt: update_cache=yes

  roles:
    - ANXS.mysql
    - ANXS.postgresql
    - ANXS.utilities
    - DavidWittman.redis
